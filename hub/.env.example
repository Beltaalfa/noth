# Hub - Portal Clientes + Admin
# Copie para .env e preencha os valores
# NUNCA versionar .env - use apenas .env.example como modelo
# Produção: https://hub.northempresarial.com

# URL do Hub (obrigatório para NextAuth - sessões e callbacks)
# Produção:
NEXTAUTH_URL=https://hub.northempresarial.com
# Desenvolvimento local:
# NEXTAUTH_URL=http://localhost:3000

# Gere com: openssl rand -base64 32
NEXTAUTH_SECRET=generate-a-random-secret-with-openssl-rand-base64-32

# Banco de dados PostgreSQL do sistema
DATABASE_URL="postgresql://usuario:senha@localhost:5432/hub_db?schema=public"

# Criptografia para senhas de conexão (opcional, use openssl rand -hex 32)
ENCRYPTION_KEY=generate-with-openssl-rand-hex-32
